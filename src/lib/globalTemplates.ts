// Minimal stub — getGlobalTemplateChecklist is only used by BulkAddToMyTemplatesDialog
// which is itself a stub in this project.
export function getGlobalTemplateChecklist(_id: string) {
  return null;
}
